# Bachelorproef Jonas De Vrient
## Verschillende benaderingen van State Management in Flutter

Compile to pdf by running following command:
```sh
pdflatex -synctex=1 -interaction=nonstopmode --shell-escape %.tex
```